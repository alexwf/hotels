@model SisEventos.Models.Suite;
@{
    ViewData["Title"] = "Delete";
}

<h2>Apagar suíte</h2>

<p>Tem certeza que quer apagar a suíte @Model.Tipo ?</p>

<form asp-controller="Suites" asp-action="Delete" method="post">
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <a asp-area="Admin"
               asp-controller="Suites"
               asp-action="Index"
               class="btn btn-default">Cancelar</a>
            <button type="submit"
                    class="btn btn-danger">
                Apagar
            </button>
        </div>
    </div>
</form>

